def sort_pairs():
    """Make a function that recieve a tuple with two integers
        and return a sorted tuple in ascending maner if first
        tuple member is greatest then second.

        From hexlet.io Python Basics Lesspn nr.9 Tuples.
    """

    my_tuple = tuple(my_tuple)
    my_list_from_tuple = list(my_tuple)
    return tuple(sorted(my_list_from_tuple))

